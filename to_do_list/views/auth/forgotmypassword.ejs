<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head.ejs') %>
        <title>Forgot Password</title>
        <link rel="stylesheet" href="../../public/css/auth/forgotmypassword.css">
</head>

<body>
    <div class="forgot-password-card">
        <h4 class="text-center mb-4">Şifremi Unuttum</h4>
        <p class="text-muted text-center">
            Aşağıya e-posta adresinizi girin, şifrenizi sıfırlamanız için size talimatlar gönderelim.
        </p>
        <form method="post">
            <% if (typeof message=="undefined"|| classs=="danger") { %>
                <% if (typeof classs!="undefined") { %>
                    <% if (classs=="danger") { %>
                        <div class="alert alert-danger" role="alert">
                            <%= message %>
                        </div>
                    <% } %>
                <% } %>
                <div class="mb-3">
                    <label for="email" class="form-label">E-posta Adresiniz</label>
                    <input type="email" class="form-control" id="email" name="email" placeholder="ornek@gmail.com" required>
                </div>
                <button type="submit" class="btn btn-primary w-100">Sıfırlama Bağlantısı Gönder</button>
            <% } %>
            <% if (typeof message!="undefined"&&classs=="success") { %>
                <div class="alert alert-success" role="alert">
                    <%= message %>
                </div>
            <% } %>
        </form>
        <div class="mt-3 text-center">
            <a href="/login" class="text-decoration-none text-primary">Giriş Yap</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>